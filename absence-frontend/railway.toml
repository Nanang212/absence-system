# Railway Deployment Configuration for Frontend

[build]
builder = "dockerfile"
dockerfilePath = "absence-frontend/Dockerfile"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on-failure"
