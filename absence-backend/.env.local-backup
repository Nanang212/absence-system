# Database Configuration (PostgreSQL)
DB_CONNECTION=postgresql
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=absence
DB_USERNAME=postgres
DB_PASSWORD=admin

# Convenience DATABASE_URL for Prisma (PostgreSQL)
DATABASE_URL="postgresql://postgres:admin@localhost:5432/absence?schema=public"

# Remote attendance endpoint base URL (example)
REMOTE_BASE_URL=https://metrodata.peopleshr.com
REMOTE_LOGIN_PATH=/hr/security/login?ReturnUrl=%2fhr
REMOTE_MANUAL_SWIPE_PATH=/hr/TNAV9/api/ManualSwipe/SubmitManualSwipe
REMOTE_SHIFT_CODE=000002

# App
PORT=3000
APP_SECRET=replace_with_a_strong_secret_here

# Telegram Bot (untuk notifikasi absensi)
# Cara setup: 
# 1. Buat bot baru via @BotFather di Telegram ✅
# 2. Dapatkan bot token dari BotFather ✅
# 3. Buat grup/channel untuk notifikasi ✅
# 4. Tambahkan bot ke grup ✅
# 5. Dapatkan chat_id grup (bisa pakai bot @getidsbot atau @RawDataBot) ✅
TELEGRAM_BOT_TOKEN=8364246819:AAEwnPQcIpWEEsu-aiGNKgP_p3m5QsPJUis
TELEGRAM_CHAT_ID=-5095396407

# WhatsApp Bot (untuk notifikasi absensi via WhatsApp)
# Cara setup:
# 1. Jalankan backend dengan npm run start:dev
# 2. Akan muncul QR Code di terminal
# 3. Buka WhatsApp di HP → Settings → Linked Devices → Link a Device
# 4. Scan QR code yang muncul di terminal
# 5. Buat grup WhatsApp untuk notifikasi (contoh: "Absensi MII Jasamarga")
# 6. Tambahkan nomor WhatsApp yang sudah di-scan ke grup tersebut
# 7. Isi nama grup di bawah ini (HARUS SAMA PERSIS dengan nama grup)
WHATSAPP_GROUP_NAME=Absensi MII Jasamarga
